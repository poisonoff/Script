/**
 * Pocket Casts Plus Unlock (Protobuf Version)
 * 作用：拦截二进制响应并替换为已激活的高级会员数据
 */

const url = $request.url;
const method = $request.method;

// 你提供的包含 Plus 会员信息的原始 Hex 序列
const plusHex = "";

function hexToUint8Array(hex) {
    let typedArray = new Uint8Array(hex.match(/[\da-f]{2}/gi).map(h => parseInt(h, 16)));
    return typedArray.buffer;
}

if (method === "GET") {
    console.log("Pocket Casts: 成功拦截订阅请求，注入二进制 Plus 数据");
    $done({
        body: hexToUint8Array(plusHex),
        headers: {
            "Content-Type": "application/octet-stream"
        }
    });
} else {
    $done({});
}
